// tests/18-discriminated-unions.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Discriminated unions', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});