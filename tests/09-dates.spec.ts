// tests/09-dates.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Dates', () => {
  it.skip('date is not supported', () => {
  });
});
