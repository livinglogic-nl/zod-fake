// tests/07-nans.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('NaNs', () => {
  it.skip('nans are not supported', () => {
  });
});
