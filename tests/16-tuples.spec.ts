// tests/16-tuples.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Tuples', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});