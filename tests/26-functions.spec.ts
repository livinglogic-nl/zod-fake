// tests/26-functions.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Functions', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});