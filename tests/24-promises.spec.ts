// tests/24-promises.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Promises', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});