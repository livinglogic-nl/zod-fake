// tests/27-preprocess.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Preprocess', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});