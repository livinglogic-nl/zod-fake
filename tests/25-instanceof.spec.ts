// tests/25-instanceof.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Instanceof', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});