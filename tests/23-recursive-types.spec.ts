// tests/23-recursive-types.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Recursive types', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});