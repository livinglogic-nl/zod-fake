// tests/22-intersections.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Intersections', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});