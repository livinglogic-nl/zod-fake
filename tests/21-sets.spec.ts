// tests/21-sets.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Sets', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});