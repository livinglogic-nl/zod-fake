// tests/11-native-enums.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Native enums', () => {
  it('native-enum is not supported', () => {
  });
});
