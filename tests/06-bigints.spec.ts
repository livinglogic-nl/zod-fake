// tests/06-bigints.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('BigInts', () => {
  it.skip('bigint is not suppported', () => {
  });
});
