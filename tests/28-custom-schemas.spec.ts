// tests/28-custom-schemas.md
import { z } from 'zod';
import { generateMock } from '..';
import { describe, it, expect } from 'bun:test';

describe('Custom schemas', () => {
  it('it works', () => {
    expect(1).toBe(1);
  });
});